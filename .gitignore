# 런타임 생성 파일
cache/
# dxdata 7일 TTL 캐시 — 없으면 자동 다운로드
config/song_db_cache.json
# 사용자별 토글 상태 — 기본값은 settings.py의 _DEFAULT_USER_CONFIG
config/user_config.json

# PyInstaller 빌드 아티팩트
build/
dist/
*.spec
*.exe

# 로그
logs/

# Python 캐시
__pycache__/
*.pyc
*.pyo

# 인증 파일 (절대 커밋 금지)
config/credentials/

# 출력 폴더
highlights/

# 환경 설정
.env

.claude/

# 개발 전용 — 프로그램 동작에 불필요
docs/
CLAUDE.md
