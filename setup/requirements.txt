# Common dependencies. torch is installed separately by
# requirements-gpu.txt or requirements-cpu.txt depending on GPU availability.
#
# NOTE: keep this file ASCII-only. pip reads requirements files using the
# system locale (cp949 on Korean Windows), so non-ASCII comments break install.
pytesseract>=0.3.10
# numpy<2 pinned: paddleocr's imgaug uses np.sctypes (removed in numpy 2.0)
numpy==1.26.4
# opencv-contrib-python 4.10 or lower: last version compatible with numpy 1.26
opencv-contrib-python==4.10.0.84
Pillow>=10.0.0
yt-dlp>=2024.1.0
ultralytics>=8.0.0
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0
PyQt5>=5.15.0
PyQtWebEngine>=5.15.0
requests>=2.31.0
browser-cookie3>=0.19.0
psutil>=5.9.0
paddlepaddle>=2.6.2
paddleocr>=2.7.0
